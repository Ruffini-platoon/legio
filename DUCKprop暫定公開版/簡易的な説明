#大将の論文「低レイノズル数プロペラの設計」をほぼそのまま実装したプログラム。
#基本的な流れとしては、constants_and_settingで設計点や諸物理定数を設定し、load_and_interpolateで翼型データを読み込み補間を行う。
#search_minimumでポーラーカーブの解析を行い局所最適解を求め、求められた関数等からrepresent_with_gammaで式を立てる。
#最後にmainで循環分布を最適化し、結果を保存。
